global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Add more jobs here to monitor other services
  # - job_name: 'my_app'
  #   static_configs:
  #     - targets: ['192.168.1.10:8080']