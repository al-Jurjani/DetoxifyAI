version: "3.8"

services:
  app:
    image: ghcr.io/al-jurjani/detoxifyai:latest
    environment:
      - ENV=production
      - UVICORN_WORKERS=4
    ports:
      - "8000:8000"
    networks:
      - monitoring
    depends_on:
      - prometheus
